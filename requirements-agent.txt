# Minimal Python deps for the agent layer (import + embeddings + smoke test).
# Does NOT pull in xgboost / pandas / numpy — those are only needed to retrain
# the xFG model, not to operate the agent against pre-exported JSON data.

pymongo>=4.6
python-dotenv>=1.0
google-genai>=1.0
